# ==================================================
# 环境配置文件 - 复制为 .env 并根据需要修改
# ==================================================

# ===================
# 环境设置
# ===================

# 构建环境: development 或 production
BUILD_ENV=production

# Flask 环境: development 或 production
FLASK_ENV=production

# ===================
# 端口配置
# ===================

# 生产环境端口
PORT=18899

# 开发环境端口（使用dev profile时）
DEV_PORT=18899

# ===================
# 重启策略
# ===================

# 容器重启策略: no, always, unless-stopped, on-failure
RESTART_POLICY=unless-stopped

# ===================
# Compose Profiles
# ===================

# 可用的profiles:
# - production: 默认生产环境（只启动主服务）
# - dev/development: 开发环境（启动dev服务和redis）
# - redis: 只启动redis服务
# - full: 启动所有服务

# 当前使用的profile（留空使用默认）
COMPOSE_PROFILES=

# ===================
# 应用配置
# ===================

# 日志级别
LOG_LEVEL=INFO

# 数据库路径（相对于项目根目录）
DATABASE_PATH=./daily_digest.db

# 数据目录（相对于项目根目录）
DATA_DIR=./data

# ===================
# 时区设置
# ===================
TZ=Asia/Shanghai

# ===================
# 网络配置
# ===================

# 容器网络名称
NETWORK_NAME=daily-digest-network

# 外部网络（如nginx-proxy-manager）
# EXTERNAL_NETWORK=nginx-proxy-manager_npm_network

# ===================
# 其他设置
# ===================

# Python路径
PYTHONPATH=/app

# Playwright浏览器缓存路径
PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright
